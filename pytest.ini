[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
markers =
    integration: calls real services (Ollama, disk); may be slow
    ollama: requires local Ollama and a small pulled model (use SAGE_MODEL_PROFILE=test)
    e2e: full workflow invoke (may mock agents); slower than unit tests
